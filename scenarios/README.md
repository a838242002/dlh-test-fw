# scenarios

Standalone Workflow YAMLs were removed in Plan 18. Chart-managed
WorkflowTemplates now live at `helm/dlh-test-fw/files/workflowtemplates/scenario/`.

Submit a scenario via the controlplane API (`dlh run <scenario>`) or
through the UI — see `docs/operations/ci-integration.md` for CI flows.
